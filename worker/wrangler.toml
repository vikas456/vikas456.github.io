name = "vikasperaka-chat"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Per-IP rate limiting. Create with:
#   npx wrangler kv namespace create RATE
# then paste the returned id below. The Worker runs without it — rate limiting
# is simply skipped — so this can be added later.
# [[kv_namespaces]]
# binding = "RATE"
# id = "paste-the-id-here"

[observability]
enabled = true
